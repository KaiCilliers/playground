package com.example.playground.util

/**
 * Constants used throughout the app.
 * Note replaced by micro-classes to avoid usage
 * of public constants that - yes - solve code
 * duplication, but it introduces coupling,
 * loss of cohesion, and it is a hard-coded
 * dependency
 */
const val DATABASE_NAME = "sunflower-db"
const val PLANT_DATA_FILENAME = "plants.json"
